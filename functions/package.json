{
  "name": "functions",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc && node -e \"const fs=require('fs'); const path=require('path'); const src=path.join('src','front'); const dst=path.join('lib','front'); if(fs.existsSync(src)){ fs.mkdirSync(dst,{recursive:true}); fs.cpSync(src,dst,{recursive:true}); }\"",
    "build:watch": "tsc -w",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^13.6.1",
    "firebase-functions": "^6.4.0",
    "internplus-portal": "file:..",
    "pdf-lib": "^1.17.1",
    "sharp": "^0.33.5",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "typescript": "~5.8.2"
  }
}
